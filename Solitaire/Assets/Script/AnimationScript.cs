using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimationScript : MonoBehaviour
{
/*    Animator anim;
    public ScoreScript leScoreScript;
    public bool patate;
    // Start is called before the first frame update
    void Start()
    {
        //leScoreScript = GetComponent<ScoreScript>();
        anim = GetComponent<Animator>();
        anim.enabled = false;
        leScoreScript = GetComponent<ScoreScript>();
        patate = GetComponent<ScoreScript>().victoirepatate;
    }

    // Update is called once per frame
    void Update()
    {
        patate = GetComponent<ScoreScript>().victoirepatate;
        if (patate)
        {
            anim.enabled = true;
        }
    }*/ 
}
